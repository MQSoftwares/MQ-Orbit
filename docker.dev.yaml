services:
  mqorbit:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3100:3100"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      PORT: "3100"
